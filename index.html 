<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Hailey Clubb, Web Projects</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    :root { --bg:#0b1220; --card:#121a2c; --text:#f5f7ff; --muted:#c2c8d6; --accent:#61d095; }
    *{box-sizing:border-box}
    body{margin:0;font-family:system-ui,-apple-system,Segoe UI,Roboto,Inter,Arial,sans-serif;background:var(--bg);color:var(--text);line-height:1.6}
    .wrap{max-width:820px;margin:0 auto;padding:32px 20px}
    header h1{font-size:28px;margin:0 0 8px}
    p{color:var(--muted)}
    .card{background:var(--card);border-radius:14px;padding:18px 16px;margin:14px 0;border:1px solid #20304f}
    a.btn{display:inline-block;padding:12px 16px;border-radius:10px;background:var(--accent);color:#041016;text-decoration:none;font-weight:600}
    .grid{display:grid;gap:14px}
    @media (min-width:700px){ .grid{grid-template-columns:1fr 1fr} }
  </style>
</head>
<body>
  <div class="wrap">
    <header>
      <h1>Web Dev Portfolio</h1>
      <p>Course projects published live for review.</p>
    </header>

    <section class="grid">
      <article class="card">
        <h2>LIVE Bait Shop</h2>
        <p>A refreshed, accessible two page site for Howard’s Lakeside General Store, includes live bait pricing, hours, and contact.</p>
        <a class="btn" href="./baitshop/">Open the project</a>
      </article>
    </section>
  </div>
</body>
</html>
